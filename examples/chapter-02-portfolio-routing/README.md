# Chapter 2: Portfolio with Reusable Components + Routing

This example sketches a small portfolio app using reusable components and client-side routing.
It assumes React Router is installed in the host project.
