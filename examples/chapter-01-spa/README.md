# Chapter 1: Single-Page Application

This example demonstrates a tiny single-page React app rendered from a single HTML file.
Open `index.html` in a browser to see the counter update without a full page reload.
