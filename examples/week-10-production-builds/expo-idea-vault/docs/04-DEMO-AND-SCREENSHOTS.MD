# Expo Idea Vault Demo and Screenshots

## Demo scenario used for documentation

For documentation verification, the app was started with Expo web, a starter template was loaded, and the draft title was edited to show an active in-progress workflow rather than a passive landing state.

## What the screenshot demonstrates

- the app successfully started in the browser preview
- starter templates are visible and actionable
- the draft form is populated and being edited
- saved ideas are present below the form
- the screen header reflects seeded local data

## Screenshot artifact note

The screenshot was generated during verification, but the binary image is intentionally not committed. The repository now keeps the demo narrative and verification details in text form so the documentation remains reviewable and git-friendly.

## Demo narration for instructors

A concise classroom narration could sound like this:

> We start with seeded ideas, load a reusable template, refine the draft, and then save or export the local data. This lets us talk about product workflow, local persistence, and production readiness in one example.

## Verification notes

The screenshot captures a realistic classroom-ready state:

- the `Maker Box` template has been loaded
- the title field was refined to show active editing
- the saved ideas list remains visible so learners can compare draft state and persisted state at the same time

## Recommended follow-up demo actions

After showing this screen, continue with:

1. changing the rating
2. saving the draft locally
3. duplicating a stored idea back into the form
4. exporting the current data as JSON

That sequence tells a complete story from ideation to persistence to recovery.
